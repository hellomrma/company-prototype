import type { NextConfig } from "next";

/**
 * Next.js 설정 파일
 * 
 * 이 파일은 Next.js 애플리케이션의 빌드 및 런타임 동작을 제어합니다.
 * 현재는 기본 설정만 사용하고 있으며, 필요에 따라 다음 옵션들을 추가할 수 있습니다:
 * - images: 이미지 최적화 설정
 * - redirects: URL 리다이렉트 규칙
 * - rewrites: URL 재작성 규칙
 * - headers: 커스텀 HTTP 헤더
 * - experimental: 실험적 기능 활성화
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
